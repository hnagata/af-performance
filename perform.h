#ifndef PERFORM_H
#define PERFORM_H

#include <vector>

bool prepare();
double sum(const std::vector<double> &a);
double pvar(const std::vector<double> &a, const std::vector<double> &b);

#endif // PERFORM_H
